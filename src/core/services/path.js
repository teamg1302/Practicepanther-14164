/**
 * API path constants for service endpoints.
 * @module core/services/path
 *
 * Centralized location for all API endpoint paths used across services.
 * This helps maintain consistency and makes it easier to update endpoints.
 *
 * @typedef {Object} ApiPath
 * @property {string} userList - Path for user list endpoint
 */
export const ApiPath = {
  userList: "/masters/user",
};
